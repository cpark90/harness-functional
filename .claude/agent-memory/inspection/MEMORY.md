# inspection 역할 메모리

조사 + 피드백 파급효과 검증 + git 담당 에이전트의 역할 특화 메모리 인덱스. 역할 정의는
`.claude/agents/inspection.md`. 그래프 지도·함정·규약을 파일로 추가하고 아래에 한 줄로 인덱스한다.

- 조사 전용: `docs/feedback/**`와 이 메모리 외 파일 생성·수정 금지. git(add/commit/branch/push)
  전담(사용자 요청 시). 피드백 파급효과는 `retrieve.py` projection + `validate.py`로 검증해
  `docs/feedback/verified/`에 보고. 조사는 `retrieve.py` pack에서 시작(전체 로드 금지).

<!-- 학습 인덱스 (한 줄씩) -->
- [refresh 판정 + 안전 커밋 절차](refresh-and-git-baseline.md) — refresh는 inspection 후속 완료까지 유지; 항목은 baseline 커밋에 보존 후 별도 커밋으로 git rm; **병렬 dispatch 중 scoped land 절차**(add -A 금지·공유 developer/MEMORY.md diff 확인·문서만 커밋이면 validate 과도기 무관)
- [물리 repo 분할 실행 (federation)](federation-physical-split.md) — cpark90/harness-ontology(중앙)+harness-data-<domain>; gh를 ~/.local/bin에 설치; 연합 validate 재현(catalog env); 중앙 커밋 선행 후 외부 확인 뒤 제거(Step5=developer); classifier git 차단은 재시도
- [연합 lockstep](federation-lockstep.md) — 중앙 core 추가·**파일 이동** 모두 recipe catalog 동반 필요; push 전 로컬 8-recipe federate 게이트(staging central symlink); recipe 추가 시 catalog+CI 매트릭스 둘 다 갱신(1:1 점검 명령 포함)
- [materialize 회귀 독립검증](materialize-regression-check.md) — worktree(직전 커밋) vs working tree 산출물 diff로 append-only/byte-identity 자체 재현; harness 인자는 bare slug
- [품질 축 감사 레시피(Q1/Q2/Q3)](quality-axis-audit-recipes.md) — validate가 못 보는 축 측정법: `reason=True` 필수(173 vs 205)·`INSTANCE_CLASSES` 미등록 7클래스/32개체·tokenEstimate fallback 15가 예산을 갉는 계산·라벨 Jaccard는 작명 패밀리 노이즈; 도구를 돌려야만 보이는 3결함(예산 조기 break·deprecated 무자각 랭킹·산출물 IRI 유출)·negative control 트리 구성법
- [web UI 쓰기 경로 감사](webui-write-path-audit.md) — 저장=삭제(82/205·375트리플) 를 **read-only**로 재현하는 3층(plan_upsert·그래프집계·개체별 SHACL); 게이트 분류 27 조용/55 거부; 브리프 수치 불신(chan-dispatch 9→**6**줄); **레지스트리 표류**가 반복 패턴이라는 진단과 불변식 1줄 처방; 개체계수 수정의 파급=worktree materialize diff + recipe별 전용 catalog로 8건 federate
- [어휘 증가 증분(원형 추가) 재검증](vocab-growth-increment-audit.md) — 연합 closure 델타 **+N 균일**이 ID충돌 탐지기; 원형↔로컬 인스턴스를 잇는 **술어가 없다**(`ho:specializes`는 Harness 전용)는 TBox GAP; `id:` 참조는 materialize만 해소하고 **retrieve 팩엔 그대로 샌다**(32/17→41/24); carrier 산출물은 "정확히 기대만큼" 바뀌었는지도 셀 것; recipes CI는 `workflow_dispatch`가 없어 중앙 push로 안 돈다
- [recipe catalog/CI glob 생성 land](recipe-catalog-glob-land.md) — `gen_recipe_catalog.py`가 디스크 `recipes/*/`에서 catalog+CI matrix 생성(central 블록은 중앙 catalog `central/` prefix 복사=REORG 드리프트 근본수리); ★land 순서 **중앙 먼저**(published CI가 central@main 생성기로 `--check`); published repo엔 dedicated catalog 애초에 없음(staging leftover, 삭제대상 0); push 전 `--repo <clone> --check`로 CI 재현; 가드 negative control 파이프 금지; B19=`gh workflow run` 수락(이전 422)+full-gate 9jobs; `docs/ci/data-repo-validate.yml` 이중 stale(L38 data/lpranging·L35 hhmm2728 owner) retired 템플릿
- [import_corpus.py 독립 재검증 레시피](importer-independent-verify.md) — 도구 land 4축 실측(결정성 diff0+코퍼스md5 read-only / ★판단성경계 grep함정: judgment술어 실배정0인데 flag주석이 6건 언급→`^\s*#` 먼저 버려라 / oracle 표본: id집합·augmentsRole·tokenEstimate(=skill.md wc-w) 일치, **유일 정당불일치=QA-gate collapse**(core:role-synthesizer 승격재사용 vs 로컬유지=②판단) / 파일럿 unchanged+validate@223); GAP=importer TestScenario/FailurePolicy 미추출(35× 복제 전 등록); refresh함정: closed 인콰이어리라도 "보존" 명시면 제거금지
- [revfactory 반영 완결성 감사(전수 대조)](revfactory-completeness-audit.md) — da4-reorg 후 방법론 delta 부품 실제 경로 지도(patterns/channels/guardrails/concepts/workflows/**verification.ttl**/tbox); 의도적 미반영 5류(step/toolAccessScope 옵션·gr-bounded-retry 통합·delta-B 중앙0사용·role-qa 권고금지)를 GAP으로 오판 말 것; 유일 GAP=cap-skill/F(그래프 주석 "later wave"가 탐지신호→refresh HOLD); 부수발견=**ho:Contract abox 개체 0**(메커니즘만 존재, EXISTING Contract는 개체 미예시); `grep "id:X a "`로 정의확인·정의≠사용 별도측정
- [recipe TTL land = staging→published clone push](recipe-land-staging-to-published.md) — recipe TTL은 중앙 커밋 대상 아님(`/staging/` gitignored); land는 `/home/cpark/git/harness-recipes` clone에 sync 후 push. staging↔published `diff -rq` **전량**(3축 외 lpranging ref-refactor 번들 주의) → 파일단위 커밋분리. per-recipe catalog scratch는 push 금지. CI가 `recipes/*/` glob 파생(d9ebf0c)이라 dir 추가삭제만 조심. classifier 회피: `commit -F`·`rm -rf` 개별·`git add` 독립. push 전 fetch+rebase
- [공개경로 스크럽 + materialize round-trip 검증(R2류)](corpus-url-scrub-roundtrip.md) — 로컬경로→canonical URL은 **prefix sed**(regeneration 아님, 판단성엣지 보존); 스크럽 무해 증명=**BEFORE(로컬+OLD materialize worktree)vs AFTER(URL+NEW+clone)** 본문 diff0·MANIFEST만 상이; ★published `central` symlink는 **미gitignore**(add 금지·후 rm); staging(`harness_ontology/staging/harness-recipes`)도 동일 sed로 published와 일치; importer+materialize는 **결합=한 커밋**(하나만 land시 stub로 깨짐); federate=`HARNESS_CATALOG`+`HARNESS_ROOT_ONTOLOGY` central validate.py
- [외부 소스 repo 조사·라이선스 게이트](external-source-survey.md) — NOASSERTION은 LICENSE 본문을 직접 읽어야(CC0/NC-ND/MIT 혼재); NC-ND는 채택 불가; 수확 우선순위는 우리 얇은 축
- [중앙 온톨로지 land + 외부 귀속 검증](central-ontology-land-attribution.md) — recipe land과 다른 lane(중앙 main 직접 push, trunk-based); taxonomy-only 노드 재검증=anti-orphan(tagged c-pattern-taxonomy 하나)+byte-identity 함정(어느 harness도 appliesPattern 안 함=grep 0 → materialize CLAUDE.md 전부 IDENTICAL, 유일 델타 harness.lock.json `individualCount` N-1→N은 정상, `diff -x harness.lock.json`으로 해석); HEAD worktree 비파괴 재현법; 첫 외부귀속=dct:source+dct:license+NOTICE 신규(중립화라 raw 복사 아님); 개별 add·commit -F·Node20 annotation 비차단
- [전면 건전성 재감사(245)](full-health-audit-245.md) — 237→245 후: 그래프 결함0(validate·determinism·cap gap0·deprecated 노드0); §2 클래스표 gap **해소 확인**(Hook/ct- 수정, 31 leaf 정위치); **잔여 GAP=§3 WorkflowStep 노드블록 미명문화**(hasStep+step*, Q3가 step 8→17·stepDependsOn/data-flow 신설, doc-only ONTOLOGYSTYLE.md); 성장분마다 §2·§3 **둘 다** 대조(부분해소 패턴)
- [전면 건전성+통일성 감사(237)](full-health-audit-237.md) — 205→237 성장 후 종합: 건전성 결함0(parity 237==237·B3/B5/B7 유지·Contract teeth 2/2·materialize 무유출); 유일 결함군=**규범문서 doc-lag**(§2 Hook행 없음·Contract 표 contract-≠실무 ct-·§3 신규술어 자리 미명문화) — 성장분마다 재발하니 감사 상시스텝으로 접두사·술어를 §2/§3와 대조; specializes 중앙은 2edge뿐("82"는 recipe-scope); oa-* te+otv 병행은 정상; authored.ttl MISSING은 webui 예약 write-target
- [harness-repo-survey Wave0 커버리지 대조](harness-repo-survey-wave0.md) — 스키마 gap 4건→**GAP-H(ho:Hook)만 확정**; GAP-E는 materialize가 이미 다중타깃(role/channel/skill/MANIFEST)이라 tools-only 강등, GAP-P/O는 부분표현(gr-least-privilege·c-traceability)+disambiguation이라 defer; inc4 importer(tools/import_corpus.py 33KB)는 이미 존재=Wave2 role경로 재사용처(선행 해소); 외부 코퍼스 미clone이라 archetype 실측목록은 Wave2 첫단계로 이월; 사용자④=cap이 온톨로지→예제로 이동(중앙=중립화 전량, raw카탈로그 금지); Wave1 즉시착수=조건부No(B17 land+validate·wshobson clone+NOTICE·retrieve tie-break 선행)
