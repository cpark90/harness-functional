# vnv 역할 메모리

verification & validation 에이전트의 역할 특화 메모리 인덱스. 역할 정의는
`.claude/agents/vnv.md`. 검증 노하우를 파일로 추가하고 아래에 한 줄로 인덱스한다.

- composition 결과물의 검증(규격 부합 = `validate.py` PASS)·평가(목적 부합 = `retrieve.py`로
  재검색·capability gap 충족·`HarnessShape` 최소구성) 판정만. 온톨로지·설계 편집 없음.
- 리포트는 `docs/verify/`에. 근거 없는 통과 금지 — 실행한 명령·측정값(도구 출력)을 명시.
- 도구는 `rdflib` 등이 있는 인터프리터로(예: `/usr/bin/python3`). 셸 기본 python3엔 없을 수 있음.

<!-- 학습 인덱스 (한 줄씩) -->
- [webui 검증 재현 절차](webui-smoke.md) — Svelte/Vite+FastAPI web UI 빌드·서버·Docker 스모크, 무결성 sha256 대조, ttl_writer dry 라운드트립, static-missing/dockerignore 결정거리.
- [composition 검증 절차](composition-verify.md) — 새 harness abox 검증 체크리스트; tokenEstimate [지킴] 범위 함정(definition-only는 범위 밖), TBox drift diff, requires→provides 그래프 직접읽기, near-synonym/omit-guardrail 판정.
- [federation 검증 재현 절차](federation-verify.md) — catalog+owl:imports 로딩(D1)·도메인 IRI(D3)·data-repo CI 게이트(D4) 판정; 로더 등가성(HARNESS_CATALOG env+모듈 reload), flat/dangling IRI 스캔, retrieve JSON엔 iri 없음, authored.ttl 의도적 부재 오탐 주의.
- [split+recipe 검증 재현 절차](split-and-recipes-verify.md) — seed.ttl→core/*.ttl 타입별 분할 무손실 증명(HEAD seed vs union instance-triple symdiff 0), recipe-repo compose(임시 central 심링크→검증→rm, union 75), redefinition 0(rdflib subject), all-11-import 판정(결함 아님·anti-rot은 projection 계층).
- [materialize (BUILD projection) 검증 절차](materialize-verify.md) — materialize.py=retrieve의 dual; recipe union 조립(임시 central 심링크→rm), 게이트 refusal(bogus id exit2/non-validating union exit1, out dir 미생성), determinism(diff -r+sha256), template-vs-fallback 구분, most_specific_types reflexive-subClassOf 잠복버그(reason=True) + 로컬 워크어라운드 판정.
- [ODR BIND+Lock 검증 재현 절차](odr-bind-lock-verify.md) — Candidate/implementationCandidate/selectionPolicy(BIND)+materialize lock(③) 판정; property-chain 타입체크(tool-docgraph≠Harness, cand는 hasComponent-reachable, subPropertyOf는 domain-trip으로 틀림), latest-stable/pinned:next cmp, lock 재현(A==B)+tamper 부분쓰기 함정(N1), 심링크는 trap 말고 마지막에 rm, union=83.
- [lpranging faithful-reflection 검증 재현 절차](lpranging-faithful-reflection-verify.md) — recipe가 REAL source harness를 충실 재반영했나(primary axis=cmp-against-source, validate 아님); materialize 산출물 byte-cmp(tools/scaffold identical, agent .md는 render라 byte-differ 정상→sanity-read), synthetic 제거(_v2/lock 부재·direct single ref·central TBox vocab는 유지), CLAUDE.md 6원칙 grep, union=81(83에서 candidate 2 빠짐), pass-with-notes(N1 language-shift/N2 recipe≠full-project/N3 vnv sim tool — 전부 경계한계). + coverage 감사 후속: **core-roles import 회귀 BLOCKER**(central roles.ttl 신설·h-multiagent hasRole 7 core role인데 recipe catalog+lpranging owl:imports가 core-roles 누락→union FAIL→materialize REFUSE; central 자체는 PASS; fix=catalog+import 2줄, 검증됨→P0 dispatch), GAP-A=domain content out-of-model 정당, GAP-B2=.claude/skills 3개 미모델(ho:Instruction 가능·low). → docs/verify/lpranging-coverage.md.
- [ODR contract-VERIFY 검증 재현 절차](odr-contract-verify-verify.md) — ho:Contract/capabilityContract/contractKind/contractCheck + verify_contract.py(level 3~4) 판정; 3링크 propertyChain 타입체크(provider≠Harness, contract는 hasComponent-rollup reachable), both loader path=96, structural grammar `..`/절대경로 escape 거부, level3 lpranging 3/3 PASS+tree破損→FAIL(validate와 별개), level4 contract-demo INV-4(v1↔v2 재바인드 pinned:next로 emitted 파일 differ·verdict 동일 2/2 PASS), executable=shell trusted-recipe 신뢰경계 note. pass-with-notes(N1 structural 얕음/N2 shell 신뢰경계/N3 재바인드 CLI無). → docs/verify/odr-contract-verify.md.
- [lpranging reference-model 검증 재현 절차](lpranging-reference-model-verify.md) — recipe가 references+explanation만 저장·concrete build doc 미저장 원칙 판정; no-stored proof(dir={ttl,README}·grep '"recipes/'=NONE·all external absolute ref), materialize fetch=byte-identical(8개 cmp·MANIFEST resolved·ONTOLOGY.md는 scaffold marker無→basename fallback), source ABSENT→exit0+3 .ref stub(단일 _resolve_ref_path·cp -rL이 central 심링크 deref 함정→scratch서 재생성), union=119·determinism·tokenEstimate NONE-missing, techdoc=aligned(central template asset ref)/contract-demo=noted tension(greeter_v* synthetic fixture 저장 acceptable·non-blocking 권고). pass-with-notes(N1 portability/N2 fixture/N3 recipe≠project). → docs/verify/lpranging-reference-model.md.
- [finer decomposition + graph-driven assembly 검증 재현 절차](finer-decomposition-verify.md) — (a)WorkflowStep+hasStep/(b)PromptSection+hasSection/(c)AssemblySection+hasAssemblySection graph-driven materialize order + role-model. anti-mistype 축=direct subProp(subject=Harness: hasAssemblySection/hasRole/hasChannel) vs propertyChain(subject=intermediate: (hasComponent hasStep)/(hasComponent hasSection)); reasoned graph서 intermediate가 Harness로 mistype 안됨 확인. **byte-identical=섹션 top-level ORDER+determinism만, BODY 아님**(a/b가 body 의도 변경→과거와 diff 정상, N2). error 3-mode 게이트 분업(dup order→validate set-level만/SHACL통과, missing→SHACL+check, unknown kind→SHACL sh:in) + materialize atomic REFUSE(out dir 미생성). redefine=자기 hasAssemblySection→order 바뀜·default 불변. role-model: userFacing present-only(orch/inspection만, worker absent)·emitted "User-facing" 줄이 2파일만·least-priv subset·channel redefinability는 TBox(class/prop def). loader symdiff 44=propertyChainAxiom BNode 재라벨(URI-individual symdiff 0이 진짜증명). union 113(scratch harness 포함시), central 110. pass-with-notes(N1 loader BNode/N2 byte-identical 정밀/N3 channel def 위치/N4 orch·design roleTool無/N5 materialize:51 stale comment). → docs/verify/finer-decomposition.md.
- [harness-100 inc3 파일럿 5 recipe 검증](inc3-pilot5-recipes-verify.md) — recipe-local blueprint 5개(21/16/31/03/46). per-recipe closure(union 금지, HARNESS_ROOT_ONTOLOGY로 한정·"1 harness assembly order"가 확증 신호, individuals 141/144/146/146/146=중앙126+로컬). materialize round-trip(recipe별 분리 out·diff -r IDENTICAL·skill byte-identical·stub 0). **★persona-ref hard-fail 비대칭 실증**: persona artifactTemplate unresolvable→resolve_template(:129) FileNotFoundError exit1 no-out(atomic), skill/impl/scaffold는 .ref stub exit0; graph PASS엔 영향0(ref는 노드 add/remove 안함). 5 recipe persona ref 43개가 임시 세션 scratchpad 절대경로→소멸시 전 recipe build blocker(finalize前 영속경로 치환 필요). **retrieve gaps=pack전역 오판 금지**(candidate-specific 아님·budget6000로 자기cap gap소멸·잔여는 co-scope 중앙harness 소유), discoverability=recipe가 자기질의 TOP인지(21=9.9/16=9.0/03=7.2/46=8.1/31=3.96). deviation정당(16 로컬qa-engineer providesCapability core:cap-synthesis 재사용/03 tool-websearch 재사용·shell無/신규cap 0=Golden Rule#2). N1=agent .md frontmatter 중첩(graph frontmatter+corpus body 자체 frontmatter). → docs/verify/inc3-pilot5-recipes.md.
- [agent memory 3-tier schema 확장 검증](agent-memory-model-verify.md) — ho:Memory⊑HarnessComponent + hasMemory⊑hasComponent(direct subProp, subject=Harness=hasRole/hasChannel 관례) + 4 discriminator datatype props + MemoryShape. 검증: reachability "all N reachable"에 mem 포함=subPropertyOf OWL RL 전파 실증(별도쿼리 불요); shape sh:in↔개체값 문자대조; **individual수 산술로 recipe 전파 실증**(중앙+4=mem3+concept1, 21:141→145/46:146→150=additive 상속·안깨짐). **★materialize byte-identity 함정**: HEAD 원시diff 금지(working-tree에 무관 uncommitted 다수)→구조적 격리(grep hasMemory/HO.Memory=0 렌더러분기부재 + 산출 CLAUDE.md 대상노드참조0 + MANIFEST symdiff=대상엔트리뿐)로 "prose 0바이트변경" 증명. **INSTANCE_CLASSES whitelist(ontology_lib.py:65)** 미등록 신규타입→MANIFEST가 HarnessComponent fallback(exit0 graceful, raise아님)=tools변경이라 developer범위밖 REC. coverage 3요소 1:1·GAP0. PASS+2 non-blocking REC. → docs/verify/agent-memory-model.md.
- [MAS 이론 재정리(W1~W3b) 검증](mas-refinement-verify.md) — ho:Agent·ho:ObservationArea(⊑HarnessComponent) composite + ho:EnvironmentSpace·ho:GlobalState(비-component) + 투영사슬 4술어. Agent패턴=WorkflowStep rollup 동형(hasAgent⊑hasComponent direct + propertyChain (hasComponent agentObservation); agentRole/Observation/Function은 subProp아님=mistype방지). **★INSTANCE_CLASSES 비대칭 규율**: component하위(Agent/OA/Memory)는 subsumption으로 계수·reachable→등록불요(brief가 등록했다해도 미등록이 옳음), 비-component(Env/GlobalState)는 등록필수. 대가=MANIFEST가 미등록 component를 HarnessComponent fallback typing(히스토그램 18=Agent5+OA10+Mem3 확인). 투영사슬 env←scopedFrom←dom←describesDomain←global←projectsFrom←OA완결(projectsFrom 실triple=area수). capacity fit Σarea≤cognitiveCapacity 눈합산(SHACL미강제=의도적). identity아님(Role·OA 별개노드). materialize prose 0바이트=grep MAS술어 materialize.py=0+산출CLAUDE.md참조0. **★recipe 카탈로그 함정**: 46은 catalog-v001 매핑無→조용히 중앙만175 오탐통과, 반드시 recipe README §Reproduce 전용카탈로그(21:190/46:195=중앙175+로컬). PASS+3 non-blocking(N1 tools/N2 대표값150000/N3 단일global-state). → docs/verify/mas-refinement.md.
- [disambiguation audit(관측 3분할+정의정리) 검증](disambiguation-verify.md) — ObservationArea 삭제→ObservationSpace(Ω_i)/AreaOfInterest(intent)/AreaOfObservation(realized) 3분할 + B~F 5쌍 정의 자기지칭·중복 제거. propertyChain **6/7/8**((hasComponent agentObservation)/(…hasAreaOfInterest)/(…hasAreaOfObservation)) = Deliverable 5번째 3-link 동형; 3클래스⊑HarnessComponent subsumption계수, hasAreaOf*/coversInterest NOT⊑hasComponent(mistype방지). ABox: agent→os-(1)→{aoi-≥1,oa-×2}, ext-oa coversInterest aoi-, Σoa tokenEstimate≤150000 눈합산. 잔재 grep(ObservationArea=0, domains-tasks+STYLE명명표까지). **DA-2 판정법**=①genus자기지칭제거②양방향Distinguished③예시비중복(B의 ReAct/plan-execute 양쪽제거가 핵심); flag(ho:notation미존재→triggerPhrase759+integrationMode734 실재하면OK). **byte-identity**=관측술어 grep materialize.py=0 + **DA-2는 class-level def라 materialize가 individual텍스트만emit** class-def특유문구 CLAUDE.md grep=0으로 미emit확증(원시diff대신 구조격리). recipe(21 catalog-v001:200/46 전용카탈로그:205=185+로컬) additive PASS. PASS+3 non-blocking(N1 내부AoO무AoI/N2 STYLE예시오탈aoi-orchestrator-external/N3 capacity대표값). → docs/verify/disambiguation.md.
- [B24 role-curator archetype + axis-linking 검증](b24-role-curator-verify.md) — central 신규 role-curator(neutral worker archetype) + carrier hasRole. **★self-report/repo 불일치 catch(2 rounds)**: Round1 developer 자평 "recipe 36 specializes 엣지(research7/design16/synth11/curator2)"였으나 실제 recipe repo 4축 타깃 0/0/0/0(committed 82는 전부 B17-family)=PHANTOM. Round2 실제 저작됨→재검증: git diff 36 lines(16/11/7/2, 25파일)+25 closure PASS+curator엣지 core:role-curator(Role/HarnessComponent) resolve+strip-test IDENTICAL=최종 PASS. 재현=`grep -rho 'specializes core:[a-z-]*'` 히스토그램+`git show <commit>|grep '^+.*specializes'`; recipe working tree clean+HEAD가 central변경보다 앞서면 그 작업 미커밋/유실. central 게이트 PASS(validate 238=237+curator only, determinism, retrieve "curate…"→Curator agent rel4.05 최상위+carrier hasRole). recipe closure=임시 central심링크→HARNESS_ROOT_ONTOLOGY=recipe IRI validate(03:261/14:259/96:265/21:258 conforms=SpecializesTypingShape 0). **★SpecializesTypingShape는 coarse**(Harness-vs-Component partition만, Role→Role은 convention). byte-identity=materialize.py grep specializes=0+strip-test diff -r IDENTICAL(materialize positional harness-id필수)+central role-curator는 h-workspace-synthesis hasRole만 참조. anti-drift PASS(def가 research/author/analyst와 판별절, §1c로 Role엔 tokenEstimate 정당 부재). GAP=carrier definition 산문 curator누락 ACCEPTABLE지연이나 graph→prose lag→follow-up권고. → docs/verify/b24-role-curator-final.md.
- [Role draft→reviewed 승격 적격성 심사](roles-maturity-review.md) — maturity 사다리 판정법. **reviewed 바=코퍼스 recurrence 플로어**: 이미 reviewed인 neutral archetype 중 최약 recurrence(research=7 committed specializes 엣지)를 promote 하한선으로 삼음(synth 11/design 16). 재현=recipe repo `git grep -hoE 'specializes core:role-[a-z-]*' HEAD`+working tree clean 확인(B24 phantom 방지). 판정: analyst29/author22/implementer11/strategist8 → **promote**; tester3(+self-declared salience0.25 keep신호)/planner2/curator2(B24가 의도적 draft 착지)/coordinator0(recipe recurrence 없음·h-peer-mesh 단일 carrier뿐) → **keep-draft**. **★maturity ripple**: materialize.py/ontology_lib.py=maturity 0참조(emit-byte 불변). retrieve.py=①lifecycle_factor는 **deprecated만** 핸디캡, draft/reviewed 둘다 1.0=랭킹 불변 ②pack JSON "maturity" 필드 리터럴만 draft→reviewed 반영(cosmetic, 배지는 deprecated만). ⇒ 승격은 rank-neutral+emit-byte-neutral, TTL 4토큰 intended edit(byte-identity 아님). 승격 concrete agent(orch/dev/vnv/inspection)는 recurrence 아니라 repo자체 agent라 별도 근거. → docs/verify/roles-maturity-review.md.
- [role consolidation(node 병합/삭제) 검증](role-consolidation-verify.md) — 두 role 병합 최종상태 독립검증; dangling 0(주석포함)/guardrail orphan 0(거버넌스gr 다른carrier로 reachable)/agent→neutral archetype 인스턴스화(agent-developer→role-implementer)/rebind 중복금지(이미있던 carrier는 remove-not-add, dup체크)/count HEAD16→14 -2/recipe 동명 local노드는 core: 아니면 무해+closure validate(catalog core-roles매핑)/retrieve 흡수기능 히트. **★concrete/neutral GAP=pre-existing 구분**(agent-synthesizer∈h-multiagent.hasAgent인데 role-synthesizer∉hasRole, agentRole↔hasRole SHACL미강제=validate무해·HEAD확인=회귀아님→residual만). 새 주석 그래프불일치+materialize docstring doc-lag=cosmetic N. → docs/verify/role-consolidation-final.md.
- [Concept vocab 전수 감사(38 skos:Concept)](abox-audit-concepts.md) — concepts.ttl near-dup/orphan/계층 감사. tagged 카운트=rdflib union `ho:tagged` object별(셸python3엔 rdflib無→/usr/bin/python3). **orphan 기준=0-tagged**(38/38 tagged≥1→orphan 0, validate "all 236 reachable"와 정합). **★anti-drift FIRST 관철**: near-dup 5군집(synthesis↔cross-validation/execution-mode↔pattern-taxonomy↔complexity-governance/dispatch↔delegation/controlled-vocab↔reuse-first/composition↔reuse-first) 전부 정의에 "Distinct from…"/"orthogonal"/"independent of" 자기판별 문구 박힘→merge 0건 KEEP. 진짜 결함 1건=**c-autonomy 계층 앵커 누락**(subject-matter top 9개 중 유일하게 topConceptOf도 broader도 없이 skos:related c-safety만; tagged=2 wf-react/h-coding라 validate orphan 통과하나 SKOS 계층 floating)→KEEP-but-wire(topConceptOf id:scheme 추가). low-usage tagged=1 다수는 결함아님. → docs/verify/abox-audit-concepts.md.
- [patterns/mode/assembly abox 정리 감사](patterns-abox-audit.md) — predicate-vs-prose grep 함정, 양-repo(중앙+recipe) 소비 스캔, catalog-stock≠orphan(bind0이나 tag로 reachable=neutral parts 재고), deprecated 묘비=이미 mode-*로 supersede된 병합기록(KEEP), peer-mesh(pattern)↔agent-teams(mode) 직교=anti-drift(h-peer-mesh 이중바인딩+TBox:253 실증), AssemblySection order 1..13 다줄목록은 validate로 확인, near-synonym 3등급(명시대조=KEEP/구분축만=clarify/MERGE아님), REMOVE·MERGE 0.
- [abox 정리 감사 — Guardrail(40개)](abox-audit-guardrails.md) — guardrails.ttl near-syn/orphan/merge 감사. 참조카운트=rdflib union `hasGuardrail`(harness)+`roleGuardrail`(role) object별(guardrail은 concept-tag 아니라 **바인딩**으로 reachable; 셸python3엔 rdflib無→/usr/bin/python3). **orphan 기준=total(hasG+roleG)=0**; 40/40 total≥1→orphan 0(validate PASS 정합), 저사용 total=1 다수는 h-harness-factory 메타하네스 wiring이라 neutral-parts결함 아님. **유일 진짜중복=gr-no-nested-teams→gr-depth-limit**(leaf-worker 제약의 team-vs-delegation 어휘 재진술·문서화 구별 없음·둘다 draft·h-harness-factory 단독 co-bind, 생존=정량≤2 depth-limit). KEEP-but-clarify 2(grounding altLabel 과확장·flatten-hierarchy=depth-limit 연질쌍 선호vs cap). **★anti-drift FIRST**: promptText "Distinct from id:gr-…" 있으면 KEEP(human-checkpoint/integration-coherence/well-formed-skill↔ins), concrete/neutral 쌍 KEEP(delegated-orchestration은 execution-separation의 orchestrator-concrete near-subsumed이나 참조도 상이); 판별자=대상(cite주장↔출처/grounding artifact↔rationale/controlled-vocab term/reuse-first part) 또는 축(root-cause깊이 vs generalize범위). KEEP37/clarify2/MERGE1/REMOVE0. → docs/verify/abox-audit-guardrails.md.
- [abox 정리 감사 — 구조/행위 나머지 클래스](abox-structural-audit.md) — Capability9/Channel6/Workflow7+Step10/Tool4/SystemPrompt4+Section3+Instr1/Task6+Domain4/Deliverable2/Memory3/ModelConfig2/Obs(Space5·AoI5·AoO10)/FailurePolicy8+Scenario2/Contract2/Harness7/Constraint1 전수. **★orphan은 grep 말고 validate reachability("all 236 reachable")로 전 클래스 일괄 닫음**. Capability 9/9 provides↔requires 매칭(비-Tool 제공자: cap-traceability←gr-traceability/cap-synthesis←role-synthesizer/cap-skill←ins). **redundancy 0/orphan 0**: 근접 3쌍 전부 정의 판별절로 KEEP(chan-workspace↔task-board=아티팩트vs task-claiming, fp-dispatch-timeout↔agent-failure-retry=침묵vs보고, h-3carrier=동일 base지만 축상이+byte-identity). non-blocking flag 3(label-only Cap 5종·AoI/AoO tokenEstimate 비대칭§1c상 무해·브리프 카운트stale=SystemPrompt실측4≠7/Instr는guardrails.ttl/Deliverable는workflows.ttl). → docs/verify/abox-audit-structural.md.
- [abox 정리 적용분 재검증 절차](abox-cleanup-apply-verify.md) — 감사 권고(MERGE/clarify/정의추가)를 developer가 적용한 뒤 self-report 없이 재현 판정. **MERGE 검증=삭제노드 원 promptText(`git show HEAD:<ttl>|grep -A6 <id>`) vs 병합후 텍스트 의미대조**(no-nested-teams "leaf agents not leaders"→depth-limit "leaf agents that do not spawn **or lead**"=흡수증거·의미손실0). dangling 0(grep=0)+흡수노드 carrier에 이미 co-bound면 remove-only가 정답(1회출현·무중복). prefLabel→altLabel 승격+tokenEstimate 증가정합. **순delta 산술**: 워킹트리 절대개체수는 누적uncommitted라 baseline 재현불가→cleanup 5파일 diff의 typed-individual 추가/삭제만 카운트(리터럴수정=delta0, MERGE1=−1). clarify/정의추가는 리터럴이라 위상무영향(capability provides↔requires 매칭 validate축+retrieve `requires:` 해석, §1c altLabel/definition은 tokenEstimate 범위밖→불변정상). pattern 대조절 prose 참조 id는 typed-decl grep(validate는 prose ref 미검출). KEEP노드 불변=diff 접촉목록만 확인. **★워킹트리 격리 함정(N1상습)**: diff stat 스코프밖 파일=prior 검증완료 태스크(role-consolidation ripple/B24) 귀속으로 판별·본태스크 결함아님·inspection 커밋분리 판단거리, 세션시작 git status는 stale. PASS+N2(개체수 baseline). → docs/verify/abox-cleanup-verify.md.
- [Q1 uniformity linter 검증 재현 절차](q1-uniformity-linter-verify.md) — lint_uniformity.py 5축(tokenEstimate§1c/prefix§2/language§1d/maturity/definition) 독립검증; (4)(5)는 shapes 파생(targetClass+path+minCount≥1, 실측 maturity 9클래스·def 1=Memory, sh:node간접 0), §1c 6클래스 원문일치·PromptSection/WFStep/AoO제외옳음(§3별개축), teeth=in-memory 복사본 1건주입→해당 check함수 1건+cross-axis격리, 비-vacuous scope(token55/prefix245/lang546/mat43/def3). ★latent FP(Note-2): §1c "promptText있는" 조건부인데 4클래스 무조건요구·현재 promptText-less=0라 오탐0. ConceptScheme 싱글턴 미검사=Note-1(skos타입이라 instance_nodes밖). read-only(validate PASS+ontology클린). PASS+2note, CI적합. → docs/verify/q1-uniformity-linter-verify.md
- [Q3 세분화 감사(blob 다중책임 판정)](q3-granularity-audit.md) — 판별축=길이 아닌 "여러 책임 혼합"([지킴]§1); ★분해 하위클래스는 WorkflowStep/PromptSection/AssemblySection 3종뿐 → Role/Channel/Pattern/Mode/FailurePolicy/Guardrail은 원자적어휘(쪼개면 anti-drift 신규클래스 drift). NS=schema#/id/core(w3id아님). Harness 4종=이미 hasRole/hasAssemblySection 분해완료·정의는 overview산문(ACCEPT). 비-Harness 상위=단일개념+Distinguished from 판별절(ACCEPT). gr-design-for-loss=4facet 단일 doctrine(C-0확정). DECOMPOSE 0건=세분화 충분. → docs/verify/q3-granularity-audit.md
- [deprecated-maturity 은퇴 + planning-doc 재동기화 검증](housekeeping-deprecated-retire-verify.md) — behavior-preserving refactor 판정; **핵심=HEAD worktree(`git worktree add --detach HEAD`)로 변경전 격리→ontology_lib.ROOT=__file__이라 각 retrieve가 자기 worktree ontology 로드=retrieve+tbox 결합 end-to-end 8팩 diff byte-identical**, PYTHONHASHSEED=0 고정으로 hash-noise 제거. **함정: `--format`은 md|json만("markdown"→argparse 거부 양측 동일오류 오탐)**. 전-축 은퇴 grep은 tools/를 `--include="*.py"`로(webui/static 2MB minified 히트). 잔여 "deprecated"=guardrail promptText 정책 산문+webui 번들(무관). 제거 심볼이 부르던 헬퍼(maturity_values) 존치·타용도 사용=orphan 아님. OPEN-ISSUES=land해시 git log subject대조+미해결 표본 grep+헤더 245=validate reachable. PASS. → docs/verify/housekeeping-deprecated-retire-verify.md
- [recipes 품질 진단 probe](recipes-quality-probe.md) — central Q1/Q2/Q3+dangling 렌즈를 38 recipe에 적용. 임시 `./central` 심링크(gitignored, rm 후)→per-recipe union validate/lint. **판정=(b) 작은 실제 큐 1종**: `ho:Contract` 노드 5개(lpranging 3+contract-demo 2)가 `contract-*`인데 §2는 `ct-`(ONTOLOGYSTYLE:135) — recipe convention 아닌 진짜 drift(둘만 a ho:Contract=ODR 증분 slip), validate 못보고 린터만 잡음. 나머지 전부 정합: validate 38/38 PASS, Q1 token/lang/mat/def 0, Q3 decompose 0(length≠defect), dangling 0. **함정**: B23 stale는 node-level(같은 subject가 specializes core:+negation-def)로만=0, file-level grep 전부 오탐(negation은 domain/fp, specializes는 role 별개노드). recipe id:role-developer는 local(core: 아님)=삭제참조 아님. Q1 린터는 class-based라 recipe에 적용가능(오탐 아님).
- [Wave-15 mass recipe import 검증](wave15-recipe-import-verify.md) — importer-생성 draft recipe 배치(harness-100 커버리지) 재현. 임시 central 심링크→per-recipe closure validate(HARNESS_ROOT_ONTOLOGY)→rm; tools는 CENTRAL(recipes repo엔 tools/無). **★G3 target resolution=SpecializesTypingShape 초과 필수축**(shape는 coarse Harness/Component 파티션만·unresolved도 통과 가능)→rdflib로 id:<slug>/ 주체의 specializes object가 ho:Role typed·recipe-ns 밖인지(62/62 resolved 0 bad; 클로저에 딸려오는 중앙 h-support→h-research Harness엣지는 recipe-authored 필터로 배제). 미emit=grep materialize.py specializes=0. dangling core: 오탐=FLAG주석 `core:dom-*` 산문(15×). G5 결정성=importer stdout 2회 sha256 동일+committed vs fresh diff는 ADDITIVE만(specializes+로컬 dom/task). G6=domains-tasks.ttl 미수정=중앙어휘 신설0·reuse 2개(23/63) pre-exist. G7 손상소스(27/88)=SOURCE-QUALITY NOTE+corrupt토큰(inthisbefore) source∧fresh-import 존재로 verbatim 증명. anti-drift SKIP=Σ`a ho:Role`74−Σspecializes62=12; name유사≠semantic(critic-synthesizer NOT→synthesizer)이면 정당. PASS+N1(analyst-flavored SKIP 몇개 core:role-analyst 클린핏이나 보수적skip=수용). → docs/verify/wave15-import-verify.md
- [recipes 전수 통합·format 감사(53 recipe)](recipes-consolidation-audit.md) — cross-recipe 중복통합(축①)+format(축②). 각 recipe TTL standalone parse(union 금지). **축① 통합후보 0건**: exact-normalized/J≥0.85/J≥0.60 실질텍스트 dup 전부 0(persona 유사=importer skeleton 공유일뿐 J<0.60=neutral-parts 대전제); 유일 near-cluster=FailurePolicy refer-to-expert 3종은 이미 specializes core:fp-refer-to-expert 라우팅=KEEP; TestScenario 대군집=prefLabel skeleton만 공유·본문 도메인특화=instance-local KEEP; Guardrail/Channel/Workflow local 0=완전통합. recipe-local vs 중앙 core J≥0.75=0(재저작-of-central 없음). **축② teeth 0/53**(PREFIX_MAP/Hangul regex를 lint_uniformity.py:106,126서 복제해 standalone 재구현=린터와 동일판정; contract-* drift는 ct-로 수정확인). metadata=provenance계열 내부일관(importer50=source+license/handmade3=둘다없음 first-party). **cosmetic 1건([지킴]밖)=scenario prefLabel 대소문자 drift 5 recipe(81/82/87/88/90 sentence-case). grounding=techdoc/88/21/100 closure validate PASS. 함정: grep -q dct:source가 주석 오탐→`^[^#]*` 필터. → docs/verify/recipes-consolidation-audit.md.
- [RECIPE_STANDARD rollout 최종 검증](recipe-standard-rollout-verify.md) — 15-recipe fp 백필+53 fleet conformance+spec-fidelity flag 독립재현. **★핵심발견: 실제 ho:HarnessShape(shapes:21)는 prefLabel+Domain+Task+SystemPrompt+Workflow만 강제·Tool/Guardrail/Model 안함** → importer skeleton(judgment binding 유보)이 validate PASS. 백필 15=fresh skeleton이라 §1 hard-core 10중 5(usesTool/hasGuardrail/usesModel/requiresCapability/tagged) 결여=38/53 → **RECIPE_STANDARD §0(53/53)·§1(SHACL가 Tool/Guardrail/Model 강제) 둘 다 부정확=CONCERN**(graph결함 아님, standard文서vs fleet 불일치). ① fp 백필 clean: dangling 0/local fp 구조 0bad/**B23 node-level 0**(negation-text 있으면 specializes=[], specializes 있으면 no-claim=올바른 판별)/fabricate 0(27·88 손상소스 verbatim+accepted-omission이 comment일치, 41 local fp가 source `## Error Handling` 1:1). ② lint 0/53(중앙 check 함수 import→recipe standalone graph 직접호출), validate 53/53, run-behaviour Role/Instr/ExecMode 51·TS 44·FP 49, reduced-profile 예외=techdoc+contract-demo, §2 accepted-reason=header FLAG 주석, §5 fix landed(scenario case 81/82/87/90+contract-*→ct-*). ③ FAILURE-MISSING 오탐 진짜(regex `^##\s+error handling\b` vs source `## error` word-drop), 교정 타당(fp IRI재사용=fabrication0/scenario word-salad라 미복구=옳은 비대칭), importer경화=중앙 tools 후속. nit=27 header FLAG stale(fp 바인딩됨). PASS-with-CONCERN. → docs/verify/recipe-standard-rollout-verify.md
- **[↑ FINAL close-out(같은 파일 하단 절)]** 위 CONCERN 후속 재검증: hard-core 5술어 이제 53/53(15 skeleton 5/5·74 role 4술어 전수), §1 SHACL 과장 고쳐짐 → **두 축 RESOLVED**. 하지만 spec 정정이 §0 run-behaviour 숫자를 새로 틀리게: execMode 51(주장52)·TS 44(주장51,off7)·FP 49(주장52) — §2 자체 accepted-omission과 모순. 함정: **spec 수정 검증 시 SHACL줄만 보지말고 §0 coverage 전부 rdflib 재집계**. ho: NS=`/schema#`(not `/ho#`). PASS-with-CONCERN(doc-only). → docs/verify/recipe-standard-final-verify.md
- [용어정립(리터럴-only) 적용분 검증](terminology-apply-verify.md) — 위상 무변경·literal-only 적용 유형 판정법. validate PASS는 거의 자동이라 증명축이 딴 데: ①HEAD worktree vs WT **abox instance-triple symdiff**(removed==added==N·subject집합 동일·predicate가 리터럴술어뿐)+prefLabel 전수 changed=0 → id/label 불변 기계증명 ②discoverability는 **편집 前 문면 쿼리**로만 잼(노드집합·candidates·budget_used 불변이면 relevance 소폭 하락은 퇴행 아님; HEAD worktree retrieve.py가 자기 ontology 로드=진짜 before/after, PYTHONHASHSEED=0) ③skos:definition은 **emit 대상**(materialize.py:484 WF/:502 Pattern/:531 Role)이라 byte-identity 주장 금지→`diff -r`로 "의도한 문장에만 한정" 증명(h-multiagent=3문장+lock count뿐). **★함정: retrieve JSON `nodes`는 list·`id`는 full IRI(endswith 매칭)·`candidates`엔 id 없음(label+relevance)** — 키 오독하면 거짓 FAIL. 산문 grep "N곳"은 원 보고 믿지 말고 재집계(6 주장 vs 실제 9). **공지시 클러스터**(agent-X/os-X/aoi-X/oa-X가 한 문장 공유)는 계획이 하나만 지목해도 같이 고치는 게 옳음=정당한 초과. tokenEstimate: §1c는 존재만 요구·산식 무규정→정의문 길어져도 비차단, budget_used 불변으로 무해증명+chars/4 갱신값 권고, 과소평가가 HEAD부터면 이번 결함 아님. pass-with-notes. → docs/verify/terminology-apply-verify.md
- [online-agent 웨이브 검증(축 신설+step renumber+동시성 배선+emitter)](online-agent-wave-verify.md) — 4개 사용자 요구를 한 웨이브로 반영한 유형. **★A 기판(substrate) 미선언 축**: `gr-work-claim` promptText가 "shared task board"를 전제하는데 유일 carrier h-workspace-synthesis의 hasChannel=chan-workspace뿐, chan-task-board는 h-harness-factory 소유 → validate·린터 통과, **materialize 산출물에서 "지시문이 참조하는 채널/패턴이 같은 문서 Channels/Process 섹션에 실재하는지"** 로만 잡힘. 브리프가 "harness X는 board 미선언이라 의도적 미배선"이라 하면 **배선된 Y도 같은 조건인지 확인**(사유 비전이). 단 pat-fanout-fanin 무배선은 pre-existing catalog-stock(13 pattern 중 7개 동일·tagged로 reachable)=회귀 아님. **★B 주석 taxonomy 주장은 전수 대조**(ACTIVATION 축 "standing은 orch·inspection뿐"→전 16 Role roleGuardrail 히스토그램 2/13/1 일치+userFacing 동반). **★C renumber는 DAG 3중**(stepOrder 1..N contiguous + dangling stepConsumes 0 + stepDependsOn 단조) — 전부 validate 밖; 정의문 명사구("from the intent profile")가 stepConsumes에 없는 **산문-엣지 괴리** 함정. **★D emitter 격리**=HEAD worktree 양쪽 materialize `diff -r`→그래프분 제외 후 산문 변경이 의도한 1줄뿐. **★E 개체수 self-report 재측정**(브리프 253→261/+8 vs 실측 245→250→261/**+11**; validate 헤더는 skos:ConceptScheme 제외라 rdflib보다 1 작음). 수치: Concept은 tokenEstimate·maturity 둘 다 없음이 정상(41/41), ExecutionMode 4형제 tokenEstimate<chars/4 편차 정상, 전그래프 동일 prefLabel 13=클래스 교차 관례, **retrieve는 skip-not-break라 `truncated` 필드 없음·900/900은 절단 아님(동점 IRI tie-break)**, TBox step 술어 6종뿐(step↔channel 없음=out-of-model 정당). 판별절은 emit되는 값 안에 있어야(주석은 미emit); 상반 guardrail 병기는 각 promptText가 자기 범위 명시하면 비-이슈. PASS-with-notes(F1~F4,N5,N6). → docs/verify/online-agent-wave-verify.md
- [TBox 축 신설 + shape 조이기(minCount) 검증](tbox-axis-tightening-verify.md) — `ho:memoryWriteTiming`(write-side 축)+`sh:in` 3값+`minCount 1`+altLabel+정의 보강 유형. validate PASS가 거의 자동이라 증명축 이동: ①**negative control 4종+★대조군**(닫힌값밖→In / 술어없는 신규개체→MinCount / 값2개→MaxCount / integer→Datatype+In; **(c) 같은 주입노드에 올바른 값 하나 얹어 conforms=True**를 보여야 (b)의 FAIL이 writeTiming 부재 탓임이 증명됨; 주입노드는 label/def/필수술어/carrier 결합 완비 아니면 딴 shape가 먼저 터져 오탐) ②**minCount=조이는 변경이라 recipe fleet 필수**(레시피 로컬 `a ho:Memory` 0건 grep + scratchpad에 catalog복사+central/recipes 심링크로 closure 4/4 PASS, 레시피 repo 무수정; **카탈로그 미매핑 IRI는 중앙만 로드해 조용히 PASS**=개체수>중앙 baseline으로 확인) ③**before/after는 HEAD worktree+대상 abox 1파일만 덮기**(tools `diff -r` 동일 선증명), **기여 분해**=altLabel-only 3번째 변형으로 귀속(short-term 질의 2.7→7.2는 전량 altLabel, 생산시점 문장 질의는 HEAD 미매칭→전량 정의보강) ④**emit 격리는 wt-head vs wt-after materialize `diff -r`**(MANIFEST aggregate tokenEstimate 1줄 +120=두 노드 증가분 합, 산문 byte-identical) ⑤**리터럴 보강의 대가=admission 경합**(token_cost가 tokenEstimate 직접 사용 → 기본 budget 900서 형제 tier(Firmware) 포함 3노드 탈락, `--budget 3000`서 복귀=결함아님 note). PASS+N1~N4. → docs/verify/memory-writetiming-verify.md
- [retrieve 선별 규칙(read projection 코드) 검증 재현 절차](retrieve-selection-verify.md) — `tools/retrieve.py` admission 규칙 추가 유형(`ho:alternativeOf` 영역당 1-admit). **★멀티-lane에선 순진한 HEAD worktree가 틀린 답**(타 lane ontology가 uncommitted로 섞여 26 vs 37 nodes) → worktree의 `ontology/`를 워킹트리로 **심링크**+`ontology_lib.py` 복사해 "그래프 고정·코드만 상이"로 격리, 끝나고 원복 후 DIFFER 확인=anti-vacuous. **대조군 2종**: (A) edge 제거 (B) **rule-off**(`R.alternative_clusters = lambda _g: {}`)가 더 강함=억제 주체가 데이터 아닌 규칙임을 증명+이때만 pack edges에 술어 노출. **예산 미차감은 산술로**(55 vs 135, Δ80=2×40=탈락분 token_cost 정확히=skip이 token_cost 앞). 분기 필수 4종(traversal 경유 탈락/예산-skip은 region 미선점/이웃 술어 non-trigger/reason True·False). 결정성=해시시드 5종 **× 삽입순서 6종**(`check_determinism.py`는 인스턴스 0이면 그 기능에 vacuous). 결함후보=탈락분 `done` 마킹→loser 전용 구조엣지 소실→**phantom capability gap** 실측, TBox 정의문 "lands in a later stage" **cross-lane doc-lag**, `pack["seeds"]` 잔여는 예산-skip 때문에 **pre-existing**(HEAD 빌드로 대조). PASS+N1~N4. → docs/verify/retrieve-alt-selection-verify.md
- [annotation TBox 술어 + §1c text-cap 린터 검증](annotation-tbox-linter-verify.md) — SymmetricProperty/n-ary Anchor 신설(①)+린터 cap 추가(②) 유형. **주입 negative control은 catalog에 매핑된 core 유닛(guardrails.ttl)에 append**만으로 union 진입(catalog·imports 무수정, 스크래치 복제본에서만). **★대칭 술어 SPARQL 가드는 위반 1건이 violation 2건**(양끝 focus node)=중복 아님; `sh:prefixes`는 `sh:declare` 달린 owl:Ontology 노드를 가리켜야 하고 shapes는 로더가 경로로 스킵해 union 무오염; pyshacl 0.40.0은 `advanced=False`에서도 sh:sparql 평가(가드 사망 가설 거짓); `sh:datatype xsd:decimal` 0..1은 **정수 리터럴 `1`을 FAIL**(저작 함정). **★chain axiom 발화 증명은 reachability로 못 함**(BFS가 무향) → 추론 후 그 노드를 hasComponent하는 subject가 전부 ho:Harness인지로 확인(7/7). symmetric 델타=raw +N/reasoned +2N, instance_nodes는 reason 무관 동수. **★신규 어휘 산출물 무영향은 HEAD-diff 말고 '워킹트리 복제본에서 그 단계만 되돌린 트리'와 materialize `diff -r`**(47파일 lock 포함 byte-identical; HEAD-diff는 병행 wave 귀속 설명용). cap 경계는 padding literal 길이로 제어(4C+3=PASS/4C+4=FAIL, exit 0/1). metric 사각지대 계량 필수(promptText+definition만 셈 → recoveryStrategy/scenarioExpected 무계측, 전리터럴 기준 초과 2건). 선언 tokenEstimate 산식은 median 비율로 판정(est/(chars/4)=0.90 vs est/wc=1.43 ⇒ chars/4 계열, 단 import_corpus.py:71은 wc -w). **§2 표 ↔ PREFIX_MAP 기계대조**(33행 중 Anchor만 미매핑=인스턴스 0 동안 조용히 미강제, 잘못된 슬러그 주입으로 실증). PASS-with-NOTES(N1~N6). → docs/verify/annotation-tbox-linter-verify.md
